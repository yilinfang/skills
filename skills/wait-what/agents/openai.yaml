interface:
  display_name: "Wait, What?"
  short_description: "Re-explain the last message simply"
  default_prompt: "Use $wait-what to re-explain the last message in simple terms."
policy:
  allow_implicit_invocation: false
